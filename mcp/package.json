{
  "name": "wpcom-mcp",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "start": "wrangler dev --port 8788",
    "dev": "wrangler dev --port 8788",
    "deploy": "wrangler deploy",
    "types": "wrangler types",
    "check": "wrangler types && tsc --noEmit",
    "test": "vitest run",
    "probe": "node scripts/probe-mcp.mjs",
    "oauth:start": "node scripts/wpcom-oauth-start.mjs"
  },
  "dependencies": {
    "@modelcontextprotocol/server": "2.0.0",
    "agents": "^0.22.0",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "latest",
    "typescript": "^5.9.2",
    "vitest": "^3.2.4",
    "wrangler": "^4.115.0"
  }
}
