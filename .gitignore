node_modules
dist
.DS_Store
*.local
.vite

# never commit shop credentials
.env
.env.*
*.wooenv

# editorial masters are large; only derived tiers ship
editorial-src/*.png

# vite writes these while resolving the config; they are not source
vite.config.js.timestamp-*
